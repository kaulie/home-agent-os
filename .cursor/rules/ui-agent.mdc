---
description: UI dev agent — all user-facing interaction surfaces
globs:
  # 客户端 App（原 ios/、android/）已迁出 → https://github.com/kaulie/home-agent-cli
  - plugins/chromecast-display/receiver/**
  - plugins/chromecast-display/ios/**
  - games/**
alwaysApply: false
---

# UI dev agent（`@ui`）

Fleet worker：**UI dev agent**。合并原 `@intent`（发出窗口、物流 UI）与 `@endpoint`（Cast/Receiver/呈现面）。

## 职责

- Intent Source **产品层**发出窗口：自然语言输入、语音、`intent_detail` 展示与物流时间线（**客户端代码已迁出** → [home-agent-cli](https://github.com/kaulie/home-agent-cli)）。
- Endpoint 呈现：Chromecast Receiver、Cast 协议、电视/墙上展示；见 [`docs/chromecast-cast-protocol.md`](docs/chromecast-cast-protocol.md)、[`docs/endpoint-contract.md`](docs/endpoint-contract.md)。
- 管理端 / Dev 端 UI（HomeAgentAdmin、HomeAgentDev）中与用户可见交互相关的部分。
- 游戏宿主 UI（`games/`）若涉及用户操作面。

## 不做

- Brain 规划、选边、入队 → `@brain`
- Runtime hydrate、前序门、executor → `@runtime`
- Plugin 能力实现（非 UI）→ `@capability`
- 黑盒 API 验收 → `@quality`

## 约定

- 发出端只发自然语言、只信 `intent_detail`；不注册 Edge、不执行 capability。
- Chatbox 中 `@intent` / `@endpoint` 别名解析为 `@ui`。
- 完工后 `push_msg` `@quality` 请验收（若动到对外 API 行为）。
- **发布链路**：改动必须 **git commit → 测试 → 部署/发布上线**；每个节点 Chatbox `[release]` 留痕。详见 [`release-pipeline.mdc`](release-pipeline.mdc)。未 commit 不算交付；端上 App / Receiver 发布同样要记 `deployed`（或 `skipped` + 原因）。

规范：[`docs/agent-coordination.md`](docs/agent-coordination.md)、[`docs/agent-roster.md`](docs/agent-roster.md)。
