---
description: Home Agent OS design charter; agent names and layer ownership
alwaysApply: true
---

# 设计理念（质检仲裁口径）

Home Agent OS：**异构设备上把家务贯通 感知 → 理解 → 规划 → 执行**。参与者模型见 [`docs/participant-model.md`](docs/participant-model.md)：**Participant + Role + Contract**；设备类型不是核心抽象。四种正交 Role：Intent Source / Runtime Agent / Endpoint / Observer（产品事件消费者，不是 Cursor `@coordinator`）。资源层见 [`docs/asset-contract.md`](docs/asset-contract.md)：Asset 以 `asset_id` 引用；Capability 不碰 filesystem / 永久 URL；存储与授权归 Runtime Asset Manager。一期未点名不改代码。Capability 产生执行结果；Presentation 由 Brain 决议；Endpoint 按声明的类型呈现。

- Brain 只理解与规划；模型密钥与设备执行在 Edge。例外：`kind=system` 的控制面检索/操作由 Brain 就地执行，不绑 Runtime、不做设备 I/O。
- Edge 按真实能力注册 `services[] → capabilities[]`；**每步**一个 `assigned_edge_id`（`kind=system` 步为 `system`），允许多 edge 分步 handoff。某步无在线节点具备该 capability → **入队失败**（system 步除外，始终可调度），禁止静默丢步。
- Plugin **只看本步已 resolve 的入参**。缺必填则失败，禁止从前序 step / `step_outputs` 自己去捡。
- Vision / LLM：**原始 JSON 即为契约**；结构不对就失败，禁止拆包兜底。
- 能力互相独立：`query.content` 不看图、不 TTS、不 Cast、不读墙上时钟；`clock.now` 只读本机钟；`vision.ask` 必须有图；`display.slideshow` 必须本步带 `photo_urls`。
- 发出端（Intent Source 产品层）只发自然语言、只信 `intent_detail`；不注册 Edge、不执行能力。实现者是 **UI dev agent**（`@ui`）；`@intent` 为 Chatbox 别名。
- 失败必须有可读 `msg`；空 plan 不得停在 `intent_parsed`。
- 墙上时钟用独立 `clock` 能力（`clock.now`，读本机钟，禁止 LLM）。`query.content` 仍禁止编时刻；没有 clock 或未派 clock 时必须诚实拒答「我不知道（没有报时能力）」。成功步的必填产出必须出现在 `intent_detail`。`succeeded` 却无原文 / 物流 / 产出 = 不可验收。
- 规划与控制面归 **brain agent**（`@brain`）：按步选边（允许多 edge）、`execution_timing`、入队、全部 capability 步完成后组装 `presentation`。问「现在几点了」应规划 `clock.now`，不要派 `query.content` 当钟。用户可见结果由 Brain `intent_detail.presentation` 交付；`notify.speak` 仅纯提醒或 voice TTS 交付钩；投电视仍 `display.photo` / `display.slideshow`。

会话标题见 [`docs/agent-roster.md`](docs/agent-roster.md)：`system coordinator agent` `@coordinator`；`dev controller agent` `@controller`（唯一常驻 IDE）；`brain agent` `@brain`；`runtime dev agent` `@runtime`；`UI dev agent` `@ui`；`capability dev agent` `@capability`；`quality agent` `@quality`；`deploy agent` `@deploy`；`sre agent` `@sre`；`dba agent` `@dba`。`@intent` / `@endpoint` 别名→`@ui`。

- 分层：`@brain` 规划/选边/入队/对外 API；`@capability` 改 plugin；`@ui` 改用户交互与 Endpoint/Cast 呈现；`@runtime` 改调度/hydrate/前序门；`@quality` 对外 API 黑盒 + App UI 自动化（先 XCUITest）；`@deploy` 云部署；`@sre` 运维；`@dba` 管 schema/SQL；`@controller` 编排 Fleet。跨层争议 `@coordinator` 仲裁。协调走本机 Agent Chatbox `http://127.0.0.1:8787/`（`push_msg` / `pull_msg`）。**每次 pull 前先读一遍** `docs/agent-coordination.md`。旧 Markdown 信箱已停用。
- **修复须验收：** 已报备问题实现方报完工后，必须经 `@quality` 验收（API 黑盒和/或 XCUITest，视改动面），并更新 `tests/blackbox/` 或 UI 测试报告。不得以自报结案。

# @coordinator 职责边界

`@coordinator` **不写产品代码**。做：协调与催办、争议仲裁、闲置检查、质检看板、架构/需求/文档汇总、日报催办与写入 [`docs/daily-reports.md`](docs/daily-reports.md)（每晚 22:50 催、23:00 截止）。催办写入 chat（`push_msg`），不要只写看板。

**用户对 @coordinator 说的事，若不在上述职责内：不要自己做，也不要只回「不归我」。** 同一轮 `push_msg` `@` 转到对应 handle，并在本会话说明派给了谁。分层：plugin → `@capability`；调度 / hydrate / 前序门 / 失败 `msg` / runtime 文档 → `@runtime`；发出窗口 / 物流 UI / Cast / Receiver → `@ui`；对外黑盒 API → `@quality`；规划 / 选边 / 入队 / Brain API → `@brain`；云部署 → `@deploy`；运维 → `@sre`；schema → `@dba`。跨层由 `@coordinator` 拆单后分派。

其他 agent 遇到预期外情况：**第一时间** chat `@coordinator`，由 `@coordinator` 集中处理；不要自行跨层改，也不要等下一轮轮询。

闲置超过 **1 小时** 必须 `@` 对应 handle 催办。chat **每 30 分钟**才 pull。`@coordinator` 用 30 分钟完整巡检。**禁止**五分钟心跳空转。到点若手头有活，先做完再补 pull。别人来信须 30 分钟内回复（手头工作可先做完再回）。
