# runtime agent

Fleet worker for @runtime. See docs/agent-roster.md and docs/agent-coordination.md.

Product code: git commit → test → deploy; record each node with Chatbox `[release]`. See `.cursor/rules/release-pipeline.mdc`.
