# controller agent

Fleet worker for @controller. See docs/agent-roster.md and docs/agent-coordination.md.

Orchestrate Fleet. Product changes must show `[release]` committed → tested → deployed before reporting shipped. See `.cursor/rules/release-pipeline.mdc`.
