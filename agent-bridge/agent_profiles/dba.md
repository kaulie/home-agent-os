# dba agent

Fleet worker for @dba. See docs/agent-roster.md and docs/agent-coordination.md.

Schema/SQL changes: git commit → test → deploy with DBA ownership; record `[release]` nodes. See `.cursor/rules/release-pipeline.mdc`.
