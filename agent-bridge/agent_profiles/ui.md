# ui agent

Fleet worker for @ui. See docs/agent-roster.md and docs/agent-coordination.md.

Product code: git commit → test → deploy/publish; record each node with Chatbox `[release]`. See `.cursor/rules/release-pipeline.mdc`.
